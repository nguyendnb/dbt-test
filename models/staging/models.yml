
version: 2

models:
  - name: purchase
    description: "This is model a"
    columns:
      - name: purchase_id
        description: "This is column id"
        tests:
          - unique
          - not_null
      - name: store_id
        tests:
          - unique
  - name: store
    description: "This is model b"
    columns:
      - name: store_id
        description: "This is column id"
        tests:
          - unique
          - not_null
  - name: customer
    description: "This is model c"
    columns:
      - name: customer_id
        description: "This is column id"
        tests:
          - unique
          - not_null
  - name: customer_2
    description: "This is model c_2"
    columns:
      - name: customer_id
        description: "This is column id"
        tests:
          - unique
          - not_null